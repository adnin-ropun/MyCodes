#include<bits/stdc++.h>
using namespace std;

int main(){
	string s;
	getline(cin,s);

	// char c;
	// do{
	// 	cin>>c;
	// 	// cin.get(c);
	// 	s+=c;

	// }while(c!='\n');

	// for(int i=0;i<s.length();i++) cout<<s[i]<<" ";


	// s.erase(s.begin()+2,s.begin()+4);
	// cout<<s;

	// s.insert(s.end(),3,'.');
	// cout<<s;

	// s.replace(s.begin()+1,s.begin()+4,"fna");
	// cout<<s;

	// // find pos
	// int pos=s.find("nin",0);
	// cout<<pos<<endl;

	// substr
	cout<<s.substr(2,3);
}